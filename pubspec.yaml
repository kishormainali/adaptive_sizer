name: adaptive_sizer
description: Adaptive Size helper created using concept of responsive_sizer and flutter_screen_util package.
version: 0.0.2
publish_to: none
homepage: https://github.com/kishormainali/adaptive_sizer
repository: https://github.com/kishormainali/adaptive_sizer

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
